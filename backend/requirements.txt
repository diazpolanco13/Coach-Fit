fastapi==0.115.12
uvicorn[standard]==0.34.2
httpx==0.28.1
pydantic==2.11.4
python-multipart==0.0.20
psycopg[binary]==3.2.9
psycopg-pool==3.2.6
minio==7.2.20
# bcrypt puro, sin passlib: passlib 1.7.4 es de 2020, esta sin mantenimiento y
# su backend emite warnings con bcrypt>=4. checkpw() ya compara en tiempo
# constante, que es lo unico que aportaba la capa de abstraccion.
bcrypt==4.2.1
cryptography>=42.0
# PyPI 0.1.0 salta tablas con count=0; el fix body-composition no esta publicado.
renpho-api @ git+https://github.com/danvaneijck/renpho-api.git@b259e499f125d82fa2b73b1f6eaf7be8df1775f1
