# Python
backend/.venv/
backend/**/__pycache__/
*.py[cod]
.pytest_cache/

# Node
frontend/node_modules/
frontend/dist/
frontend/.vite/

# Runtime data
backend/data/coach.db
backend/data/coach.db-*

# Media del catalogo: ~138 MB, (c) Gym visual, redistribuida por
# hasaneyldrm/exercises-dataset. La trae `python scripts/import_catalog.py`;
# no se versiona para no inflar el repo ni republicar media de terceros.
backend/static/media/images/
backend/static/media/videos/
backend/data/.dataset-cache/

# Env / OS
.env
.env.*
.DS_Store
*.log

# Logs/PIDs del arranque local (./scripts/start.sh)
.dev/

# Grafo de conocimiento de graphify: se regenera con `graphify update .`
# en segundos y sin coste de API, asi que no se versiona (1.3 MB por build).
graphify-out/
